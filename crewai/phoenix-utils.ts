export function isValidAddress(address: string): boolean {
  // TODO: Check if an address is valid on Phoenix  
}

export function convertUnits(amount: number, fromUnit: PhoenixUnit, toUnit: PhoenixUnit): number {
  // TODO: Convert between PHX and vPHX units
}  

export function estimateBlockTime(block: Block): Date {
  // TODO: Estimate timestamp of a future block
}